"""pdf-html: convert any text-based PDF into beautiful, self-contained HTML.

Structure is inferred deterministically from font metadata and geometry —
no NLP, no LLM in the core. Text is always verbatim; images are never emitted.
"""

__version__ = "0.1.1"

"""Table reconstruction.

Default: PyMuPDF Page.find_tables() (geometry-aware, handles borderless
tables); colspan/rowspan from cell geometry. Whitespace-gap heuristic serves
as fallback for the pdftotext extractor. Planned — not yet implemented.
"""
